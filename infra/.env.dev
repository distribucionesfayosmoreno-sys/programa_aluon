ENV_NAME=dev
DB_NAME=aluon_saas_dev
DB_USER=admin_dev
DB_PASSWORD=admin_dev_password_secure
DB_PORT=5433
BACKEND_PORT=8081
FRONTEND_PORT=8082
# CORS (Vercel + Cloudflare Tunnel)
APP_CORS_ALLOWED_ORIGINS=https://app-aluon-unqc.vercel.app,https://aluondev.iconseriespeliculas.xyz
# Entorno Dev (Logs detallados, DB de pruebas)

# SMTP (Gmail)
# Nota: no guardar contraseñas en git. Rellena en el servidor o en un .env local no versionado.
SPRING_MAIL_USERNAME=
SPRING_MAIL_PASSWORD=
SPRING_MAIL_FROM=
